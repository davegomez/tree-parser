machine:
  node:
    version: 4.0.0

dependencies:
  override:
    - npm install -g npm@3.5.0 babel-tape-runner
    - npm install

test:
  override:
    - npm run lint
    - npm test
    - npm run compile
